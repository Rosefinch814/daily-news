ai:
  default_provider: codex
  stage_providers:
    semantic_shortlist: codex
    selection: codex
    issue_compose: codex
    digest_feedback: codex
  timeout_seconds: 300
  repair_attempts: 1
  codex:
    command: /Applications/Codex.app/Contents/Resources/codex exec
    model:
  claude:
    command: claude -p
    model:
    max_budget_usd:
prompt:
  max_summary_chars: 650
  max_content_chars: 1200
  max_candidates: 60
logging:
  save_provider_events: true
  save_attempts: true
  append_metrics_jsonl: true
  redact_command: true
